import { TarefaConcluiaDirective } from './tarefa-concluia.directive';

describe('TarefaConcluiaDirective', () => {
  it('should create an instance', () => {
    const directive = new TarefaConcluiaDirective();
    expect(directive).toBeTruthy();
  });
});
